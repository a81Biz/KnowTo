# Puerto local del backend (wrangler dev). Cambiar solo si se altera el puerto por defecto.
VITE_DEV_API_PORT=8787

# Supabase local (docker-compose / supabase local)
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=dummy-key-for-local
