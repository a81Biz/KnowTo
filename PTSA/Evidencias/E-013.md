---
id: E-013
tipo: base_datos
origen: artifact_versions (psql — information_schema.columns)
lineas: resultado de verificación de schema real
capturada: 2026-06-14
fingerprint: SHA256-estructural-artifact-versions-schema
---

# E-013 — artifact_versions: columna `status` ausente en schema real

## Contenido capturado

```sql
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'artifact_versions' 
ORDER BY ordinal_position;
```

Resultado:
```
id                      | uuid
project_id              | uuid
product_code            | character varying
version                 | integer
artifact                | jsonb
documento_md            | text
prompt_template_id      | uuid
prompt_template_version | character varying
prompt_hash             | character varying
model                   | character varying
generated_by            | uuid
cert_score              | integer
correction_log          | jsonb
derived_from_artifact_id| uuid
created_at              | timestamptz
is_active               | boolean
```

La columna `status` NO EXISTE. El código en `certification-route` que hace `WHERE status = ...` falla con error 500.
