---
id: H-009
estado: VERIFICADA
dimension: D1
producto_id: P-007
fase_detectada: F6
severidad: MEDIA
penalizacion: 5
confidence: 95
evidencias:
  - E-009
  - E-PT210-p3-module-name
  - E-PT213-p3-canonical-names
origen_actualizacion: PT-213
---

# H-009 — Semantic drift en nombre de módulo: Temario vs P2/P3/P4

## Descripción

El nombre del Módulo 1 difiere entre los productos del mismo proyecto:

| Producto | Nombre del Módulo 1 |
|:---|:---|
| **Temario Base (ancla canónica)** | "Simulación del Proceso de Acreditación" |
| **P4 Manual del Participante** | "Evaluación Formativa Inicial" (usa nombre de unidad, no módulo) |
| **P3 Guiones Multimedia** | "Introducción al Proceso de Acreditación por Acuerdo 286" |
| **P2 Presentación Electrónica** | "Introducción al Proceso de Acreditación por Acuerdo 286" |

El Temario Base (P-007) dice que el módulo se llama "**Simulación del Proceso de Acreditación**", pero los documentos downstream lo llaman "**Introducción al Proceso de Acreditación por Acuerdo 286**" — un nombre completamente diferente que el LLM generó por su cuenta.

## Evidencia

- Temario Base (BD `temario_base`): `"nombre": "Simulación del Proceso de Acreditación"`
- P3 preview: `## Módulo 1: Introducción al Proceso de Acreditación por Acuerdo 286`
- P2 preview: `## Módulo 1: Introducción al Proceso de Acreditación por Acuerdo 286`

## Análisis de causa raíz

El LLM no está leyendo el nombre canónico del módulo desde el `temario_base` inyectado en el `enrichedContext`, o está reinterpretando/renombrando el módulo por su cuenta. El objetivo del `temario_base` es precisamente prevenir este tipo de drift, pero en este caso no funcionó para el nombre del módulo.

## Impacto

Medio. Los documentos no son coherentes en la terminología del módulo, lo que puede causar confusión en el cliente y en el evaluador CONOCER. Un evaluador que revise P3 y luego P2 verá el módulo con un nombre diferente al que aparece en el temario canónico del curso.

## Recomendación

1. En los prompts de P2, P3, P4 (y todos los F4), añadir instrucción explícita: "El nombre de cada módulo DEBE ser EXACTAMENTE el que aparece en `temario_base.modulos[n].nombre`. No lo reinterpretes ni lo modifiques."
2. En el assembler, agregar una verificación post-generación que compare los nombres de módulos contra `temario_base` y loguee una advertencia si difieren.
3. Considerar inyectar el `temario_base.modulos[n].nombre` como variable de template en el prompt (hardcoded en el contexto) en lugar de dejarlo al LLM.

## Revisión — 2026-06-15

**Estado:** ABIERTA — guardrails añadidos, regeneración pendiente

Dos capas de protección implementadas:

1. **temario.phase.ts** — `validarNombresModulos()`: detecta módulos con ≥70% similitud léxica al nombre del curso (stopwords filtradas). Loguea `⚠️ PT-111` y persiste en `temario_base.advertencias_nombres_modulos` vía `supabase.service.ts`. Implementación: **warn-not-block** (el temario se guarda aunque haya advertencias).
2. **TEMARIO_BASE.md** — Añadida regla `NOMBRE_MODULO RULE (MANDATORY)` con ejemplos de correcto/incorrecto y referencia a `{_frozen.nombre_oficial_curso}` como nombre prohibido. La instrucción aparece en el prompt de ambos especialistas A y B.

**Para cerrar este hallazgo:** El producto P-007 (Temario Base) existente en BD debe regenerarse con el prompt actualizado. Hasta que ocurra la regeneración, el dato en producción sigue teniendo el drift semántico. Estado se mantendrá ABIERTA hasta verificar que P-007 regenerado tiene módulos con nombres diferentes al nombre del curso.

## Revisión 2 — PT-204 INVESTIGATION (2026-06-21)

**Tipo:** INVESTIGACIÓN cerrada — causa raíz confirmada

**Hipótesis evaluadas:**
- H-A (LLM no-determinismo): **CONFIRMADA** — el LLM de P3 reformuló el nombre del módulo
- H-B (transformación en saveTemarioBase): **DESCARTADA** — código guarda params.temario directo
- H-C (otra fuente en enrichedContext): **DESCARTADA** — ruta trazada en document.handlers.ts:433-440

**Evidencia BD actual (proyecto ce9e3068):**
- temario_base módulo 1: "Identificación y Análisis de Requisitos del Acuerdo 286"
- P3 documento módulo 1: "Requisitos y Criterios del Acuerdo 286" ← drift confirmado
- P4 usa nombres de UNIDAD (no módulo) — comportamiento correcto

**Nota técnica:** El campo en BD es `nombre`, no `modulo` (CLAUDE.md tenía documentación imprecisa).
Ruta de acceso correcta: `previousData.temario_base.temario.modulos[n].nombre`

**Acción recomendada:** Crear PT-208 BUG/STANDARD para añadir instrucción explícita en prompt
`F4_P3_GENERATE_DOCUMENT.md`: copiar nombre de módulo EXACTAMENTE desde
`previousData.temario_base.temario.modulos[n].nombre`.

Evidencia completa: `docs/implementation/evidence/PT-204/investigation-result.md`

**Estado:** H-009 permanece ABIERTA hasta que PT-208 sea implementado y verificado.

## Revisión 3 — PT-210 (2026-06-21)

**Estado:** CORREGIDA (parcialmente) — prompt fix + _nombre_video canónico → P3 BD correcto

**Run ejecutado:** job `3fd2311d-4c81-4444-9ca9-2624a87332f8`
- Completado: 2026-06-21 19:14:12 UTC
- `validacion_estado = aprobado`

**Comparación BD (PT-210.4):**
```sql
temario_base.modulos[0].nombre = "Identificación y Análisis de Requisitos del Acuerdo 286"
fase4_produtos.P3.partes.modulo_1.nombre = "Identificación y Análisis de Requisitos del Acuerdo 286"
```
**MATCH ✅** — nombres idénticos. Drift anterior (`"Requisitos y Criterios del Acuerdo 286"`) eliminado.

**Causa del éxito:** El call manual usó `_nombre_video` con el nombre canónico directo. El extractor lo propaga al output via el template `"nombre": "{_nombre_video}"`.

**Limitación del fix (prompt):** La instrucción `productos_previos.temario_base.temario.modulos[n-1].nombre` en el extractor NO funciona en producción porque `_cargarProductosPrevios()` del frontend no incluye `temario_base` — solo P1-P8. El path siempre es undefined y cae en fallback `_nombre_video`.

**Fix pendiente (PT-212):** Modificar `step4.production.ts` línea ~1172 para calcular `_nombre_video` desde `temario_base.modulos[moduloNum-1].nombre` (dato ya disponible en el store del frontend por el gate de confirmación).

**Confianza 80%:** La corrección es real en BD para este run. El riesgo de regresión existe en producción hasta PT-212.

Evidencia: `docs/implementation/evidence/PT-210/module-name-comparison.md`

## Revisión 4 — PT-213 (2026-06-22)

**Estado:** CORREGIDA → **VERIFICADA** ✅

**Fix aplicado (PT-212.4 + corrección PT-213):**
- `_temarioData: any = null` añadido a `step4.production.ts`
- `_checkTemarioGate()` almacena `this._temarioData = res` cuando confirmado
- P3 loop usa `(this._temarioData?.temario as any[])?.[moduloNum - 1]?.nombre`
- **Bug de path corregido en PT-213:** `res.temario` es array directo (no `{modulos:[...]}`); path original `.temario.modulos[n]` era incorrecto

**Verificación BD — todos los módulos con nombres canónicos:**
```sql
SELECT datos_producto->'partes'->'modulo_1'->>'nombre',
       datos_producto->'partes'->'modulo_2'->>'nombre',
       datos_producto->'partes'->'modulo_3'->>'nombre'
FROM fase4_productos WHERE producto='P3' ORDER BY updated_at DESC LIMIT 1;
```

| Módulo | temario_base canónico | P3 BD | Match |
|:---:|:---|:---|:---:|
| 1 | Identificación y Análisis de Requisitos del Acuerdo 286 | Identificación y Análisis de Requisitos del Acuerdo 286 | ✅ |
| 2 | Simulaciones de Presentación Profesional | Simulaciones de Presentación Profesional | ✅ |
| 3 | Desarrollo de Habilidades para Persistir | Desarrollo de Habilidades para Persistir | ✅ |

Jobs: módulo 2 `d97e8134`, módulo 3 `92b9f2d2` (2026-06-22T06:21Z)

Evidencia: `docs/implementation/evidence/PT-213/pt213-2-p3-canonical-names.md`
